"""Test fixtures for the fixture / metrics / policy / AREX-XML side of ADR-010.

The prior enterprise-license autouse network stub moved with the ODR-side
tests to ``plugins/zetesis/research/tests/conftest.py`` per ADR-056 §D1
(Stage 6.3 (proper) sub-slice 1). No ODR-invoking test remains in this
directory, so the autouse stub is no longer needed here.
"""

from __future__ import annotations
